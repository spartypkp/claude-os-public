# Claude OS — Python Dependencies
# Core packages are required. Optional packages enable specific integrations.
# If optional packages fail, the system runs with reduced functionality.

# ── Core (required) ──────────────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.20.0
aiosqlite>=0.19.0
pydantic>=2.0
python-dotenv>=1.0.0
sse-starlette>=1.0.0
PyYAML>=6.0
watchfiles>=0.20.0
Jinja2>=3.1.0
croniter>=1.0.0
python-dateutil>=2.8.0
python-multipart>=0.0.5

# MCP + Claude Agent SDK
fastmcp>=2.0.0
mcp>=1.0.0
claude-agent-sdk>=0.1.0

# ── Optional: Telegram ───────────────────────────────────────────
python-telegram-bot>=20.0
aiohttp>=3.8.0

# ── Optional: Gmail ──────────────────────────────────────────────
google-auth>=2.0.0
google-api-python-client>=2.0.0

# ── Optional: Email formatting ───────────────────────────────────
markdown>=3.0.0

# ── Optional: HTML parsing (email pipeline) ──────────────────────
beautifulsoup4>=4.12.0

# ── Optional: Image processing ───────────────────────────────────
Pillow>=10.0.0

# ── Optional: macOS integrations (Calendar, Contacts) ────────────
# These require macOS and Xcode tools. Safe to skip on other platforms.
# pyapple-mcp>=1.0.0
# pyobjc-framework-Contacts>=10.0
# pyobjc-framework-EventKit>=10.0
