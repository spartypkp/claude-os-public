venv/
venv-test/
.env

# Only ignore PDFs in Documents and Logs (not Desktop)
Desktop/**/*.pdf
Logs/**/*.pdf

**/*.docx
**/*.csv

**/*.pyc
**/__pycache__/
.DS_Store
temp-src/

# Build artifacts
node_modules/
Dashboard/.next/

# Session/mission execution temp files (never committed)
.engine/tmp/

# Trash folder (soft-deleted files)
Workspace/.trash/
Desktop/.trash/

# Worker execution artifacts (reports stored in DB)
.engine/data/outputs/
.engine/data/archives/

# Async worker logs (ephemeral)
.engine/logs/async/

# Playwright skill temp execution scripts
.claude/skills/utility-playwright/.temp-execution-*.js

# Runtime state (recreated on start)
Desktop/logs/
.engine/data/db/*.db
.engine/data/db/*.db-wal
.engine/data/db/*.db-shm
.engine/state/*.log

# MCP config (contains local paths)
.mcp.json
!.mcp.json.example

Desktop/PUSH-INSTRUCTIONS.md

# Specialist workspaces (transient)
Desktop/conversations/

# Desktop templates: IDENTITY.md, MEMORY.md, TODAY.md, SYSTEM-INDEX.md
# are committed as starter templates. Once you fill them with personal data,
# add them to your local .gitignore to avoid committing personal information:
#
#   echo "Desktop/IDENTITY.md" >> .gitignore
#   echo "Desktop/MEMORY.md" >> .gitignore
#   echo "Desktop/TODAY.md" >> .gitignore
